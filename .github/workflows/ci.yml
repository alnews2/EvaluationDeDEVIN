name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  QT_QPA_PLATFORM: offscreen
  PYTHONUTF8: "1"

jobs:
  qualite:
    name: Qualité (lint, format, typage)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
      - name: Installation
        run: |
          uv venv --python 3.12
          uv pip install -e ".[dev]"
      - name: Format
        run: uv run --no-sync ruff format --check --diff .
      - name: Lint
        run: uv run --no-sync ruff check --output-format=github .
      - name: Typage strict
        run: uv run --no-sync mypy

  tests:
    name: Tests ${{ matrix.os }} / Python ${{ matrix.python-version }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        python-version: ["3.11", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@v7
      - uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
      - name: Dépendances système Qt (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            libegl1 libglib2.0-0 libdbus-1-3 libxkbcommon-x11-0 \
            libxcb-cursor0 libxcb-icccm4 libxcb-image0 libxcb-keysyms1 \
            libxcb-randr0 libxcb-render-util0 libxcb-shape0 libxcb-xinerama0 \
            libxcb-xkb1 libxcb-sync1 libxcb-xfixes0
      - name: Installation
        run: |
          uv venv --python ${{ matrix.python-version }}
          uv pip install -e ".[dev]"
      - name: Tests et couverture
        run: uv run --no-sync pytest
      - name: Publication du rapport de couverture
        if: matrix.os == 'ubuntu-latest' && matrix.python-version == '3.12'
        uses: actions/upload-artifact@v7
        with:
          name: couverture
          path: coverage.xml
          if-no-files-found: error

  paquet:
    name: Construction du paquet
    runs-on: ubuntu-latest
    needs: [qualite, tests]
    steps:
      - uses: actions/checkout@v7
      - uses: astral-sh/setup-uv@v7
      - name: Construction
        run: uv build
      - name: Vérification de l'installation du wheel
        run: |
          uv venv /tmp/verif
          VIRTUAL_ENV=/tmp/verif uv pip install dist/*.whl
          VIRTUAL_ENV=/tmp/verif /tmp/verif/bin/python -c "import calculatrice; print(calculatrice.__version__)"
      - uses: actions/upload-artifact@v7
        with:
          name: distribution
          path: dist/*
          if-no-files-found: error
